# Flags for Google Antigravity (Electron) on Wayland / Hyprland
# Place user overrides in ~/.config/antigravity-flags.conf

--ozone-platform-hint=auto
--enable-wayland-ime

# Window controls styling:
# --titlebar=hidden   # Hide minimize/maximize/close buttons (recommended for Hyprland / tiling WMs)
# --titlebar=native   # Use native GTK/Qt system window frame and buttons
# --titlebar=overlay  # Upstream default: frameless with Chromium Window Controls Overlay (WCO)
--titlebar=hidden

# Optional: Custom UI scaling for High-DPI screens (e.g. 1.5, 2.0)
# --force-device-scale-factor=1.5

# Optional: Disable GPU hardware acceleration if encountering driver rendering glitches
# --disable-gpu
